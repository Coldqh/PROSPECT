# PROSPECT 0.45.0

Usage plans, target distribution and QB open-window logic.

```powershell
cd C:\PROSPECT
# Распаковать PROSPECT-0.45.0-patch.zip с заменой файлов
powershell -ExecutionPolicy Bypass -File .\APPLY_PATCH_0.45.0.ps1
npm install
npm run test
npm run build
```
