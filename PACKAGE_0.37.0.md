# PROSPECT 0.37.0 package

- `PROSPECT-0.37.0-patch.zip` — только изменённые и новые файлы для распаковки поверх любой текущей 0.36.0; исправления hotfix v4 уже включены.
- `PROSPECT-0.37.0-full.zip` — полный проект без `node_modules`, `dist` и `.git`.
- установщик удаляет старый `src/components/career/MatchField.tsx`.
